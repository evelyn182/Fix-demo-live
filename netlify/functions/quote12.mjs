export default async (req)=>{const u=new URL(req.url),symbol=u.searchParams.get('symbol')||'EUR/USD',key=process.env.TWELVE_DATA_API_KEY;if(!key)return Response.json({ok:false,error:'Missing TWELVE_DATA_API_KEY'},{status:500});try{const r=await fetch('https://api.twelvedata.com/price?'+new URLSearchParams({symbol,apikey:key})),j=await r.json();if(!r.ok||j.status==='error'||j.code)return Response.json({ok:false,error:j.message||'Provider error'},{status:400});return Response.json({ok:true,symbol,price:+j.price,timestamp:Math.floor(Date.now()/1000),source:'Twelve Data'});}catch(e){return Response.json({ok:false,error:e.message},{status:502})}};